import { useEffect, useState } from 'react'; import { Link } from 'react-router-dom'; import { getPerformanceReviews, PerformanceReview } from '../api/performance'; import LoadingSpinner from '../components/LoadingSpinner'; import ReviewStatusBadge from '../components/ReviewStatusBadge'; import { useToast } from '../contexts/ToastContext';
export default function PerformanceReviews() { const [rows, setRows] = useState<PerformanceReview[]>([]); const [loading, setLoading] = useState(true); const { addToast } = useToast(); useEffect(() => { getPerformanceReviews().then(setRows).catch(() => addToast('Unable to load performance reviews.', 'error')).finally(() => setLoading(false)); }, []); if (loading) return <LoadingSpinner />; return <div className="card"><h1>Performance reviews</h1><p style={{ color: 'var(--color-text-muted)', margin: '0.5rem 0 1rem' }}>Reviews follow the controlled employee → manager → HR workflow.</p><div className="table-container"><table><thead><tr><th>Employee</th><th>Cycle</th><th>Status</th><th>System</th><th>Final</th><th /></tr></thead><tbody>{rows.length === 0 ? <tr><td colSpan={6}>No reviews are available.</td></tr> : rows.map(row => <tr key={row.uuid}><td>{row.employee_name}</td><td>{row.cycle_name}</td><td><ReviewStatusBadge status={row.status} /></td><td>{row.system_score}</td><td>{row.final_score || '—'}</td><td><Link to={`/app/performance/reviews/${row.uuid}`}>Open</Link></td></tr>)}</tbody></table></div></div>; }
