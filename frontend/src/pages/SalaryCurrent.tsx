import { useEffect, useState } from 'react'; import { getCurrentSalary, SalaryRecord } from '../api/salary'; import LoadingSpinner from '../components/LoadingSpinner'; import SalaryBreakdown from '../components/SalaryBreakdown'; import { useToast } from '../contexts/ToastContext';
export default function SalaryCurrent() { const [salary, setSalary] = useState<SalaryRecord | null>(null); const [loading, setLoading] = useState(true); const { addToast } = useToast(); useEffect(() => { getCurrentSalary().then(setSalary).catch(() => addToast('No current salary record is available.', 'error')).finally(() => setLoading(false)); }, []); if (loading) return <LoadingSpinner />; return salary ? <SalaryBreakdown record={salary} /> : <div className="card"><h1>Current salary</h1><p>No current salary record is available.</p></div>; }
