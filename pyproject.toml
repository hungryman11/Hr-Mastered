[tool.ruff]
target-version = "py312"
line-length = 120
exclude = [
    ".git",
    ".venv",
    "migrations",
    "node_modules",
    "frontend",
]

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]
